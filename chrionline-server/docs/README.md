# Documentation ChriOnline

PDF cahier des charges : placer ici si besoin (ex. `ChriOnline_mini-projet_cahier-des-charges.pdf`).

Outils : `JAVAFX.md` (UI, `ui/`, lancement), `MAVEN.md` (`pom.xml`, commandes).

Mini-Projet 2 (RSA → échange clé AES → AES-GCM obligatoire sur le socket, stockage optionnel) : `TUTORIAL_RSA_AES_SOCKET_ENCRYPTION.md`.

Console catalogue admin (création / suppression produits, UI dédiée) : `ADMIN_CATALOG_MANAGEMENT.md`.

Équipe : `EQUIPE-CHRIONLINE.md`, `DOCUMENTATION-COMPLETE-PAR-PERSONNE.md`, `equipe/PERSONNE-01-AUTH.md` … `PERSONNE-05-PAIEMENT.md`.
